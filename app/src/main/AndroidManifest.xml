<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="expertosbd.ferroservicios.ferroserviciosbodegasecosscannerhardware">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="com.honeywell.decode.permission.DECODE" />

    <uses-feature android:name="android.hardware.camera"/>

    <application
            android:name=".BaseApp"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".ui.main.MainActivity"
                android:label="@string/title_activity_main">
        </activity>
        <activity android:name=".ui.eventos_entrada.scanner_entrada.ScannerEntradaActivity">
        </activity>
        <activity android:name=".ui.eventos_salida.scanner_salida.ScannerSalidaActivity">
        </activity>
        <activity android:name=".ui.eventos_entrada_werner.ScannerEntradaWernerActivity">
        </activity>
        <activity android:name=".ui.TestActivity">
        </activity>
    </application>

</manifest>